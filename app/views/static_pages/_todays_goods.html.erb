
<%= link_to image_tag(object.pic_url, { :border => '0', :alt => '商材'}), primary_path(:name => object.title) %>

<h2><%= link_to object.title, primary_path(:name => object.title) unless object.nil? %></h2>

<p>
<% if signed_in? %>
	<!-- [rep]<%= object.rep_short_desc  unless object.nil? %> -->
	rep
<% else %>
	<%= object.short_desc  unless object.nil? %>
<% end %>
</p>

