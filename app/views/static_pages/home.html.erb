<% if !signed_in? %>
	<center>
		<video width="940" autoplay loop="true" >  
			<source src="https://dl.dropboxusercontent.com/u/3352587/sample.mp4"　type="video/mp4"/>
			I'm sorry; your browser doesn't support HTML5 video.
		</video>
	</center>
<% end %>

<div class="row">

	<div class="col-xs-12"> <h1>What's Popular</h1></div>

	<% for goods in @primary_goods %>
		<div class="col-xs-4">
			<div class="panel panel-default panel_custom">
				<div class="panel-body">
					<img src=<%= goods.pic_url %>>
					<%= render 'static_pages/todays_goods', object: goods %>
				</div>
			</div>
		</div>
	<% end %>

</div>


<div class="center hero-unit">
	<%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>
</div>

